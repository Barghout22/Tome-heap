import { useState } from "react";

import "./App.css";

function App() {
  // const [count, setCount] = useState(0);

  return (
    <div className="text-center bg-[url(./image_resources/booksBackground.jpg)] bg-cover h-screen">
      <h1 className="mt-auto text-4xl text-white">welcome to tome heap</h1>
    </div>
  );
}

export default App;
